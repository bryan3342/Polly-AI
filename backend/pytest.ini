[pytest]
; Only collect the headless unit suite; tests/demos holds interactive
; webcam scripts that must never run in CI.
testpaths = tests/unit
; Make `app.*` importable when running `pytest` from backend/.
pythonpath = .
