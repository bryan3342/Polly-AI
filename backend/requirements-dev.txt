# Dependencies for the unit test suite (backend/tests/unit).
# Heavy runtime deps (tensorflow, deepface, librosa, opencv) are deliberately
# NOT installed in CI — unit tests must mock anything that needs them.
pytest>=8.0
python-dotenv>=1.0.0
# The Gemini SDK is pure Python and pulls no native wheels, so the chat-service
# tests can import it in CI without dragging in tensorflow/librosa/opencv.
google-genai>=1.0.0,<3.0.0

# The transport layer is imported by the event-loop offloading tests; fastapi
# and its deps are pure Python, unlike the ML stack it transitively imports
# (those are stubbed in the test module).
fastapi>=0.104.0,<1.0.0

# Imported directly by the unit suite:
#   numpy      -> utils.serialization round-trip tests
#   sqlalchemy -> session statistics tests (in-memory SQLite)
numpy>=1.24.0,<3.0.0
sqlalchemy>=2.0.23,<3.0.0
