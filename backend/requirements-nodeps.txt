# Installed with `pip install --no-deps` — see the Dockerfile.
#
# deepface declares hard requirements on `tensorflow` and `opencv-python`. Both
# are satisfied here by deliberately chosen variants instead:
#
#   tensorflow        -> tensorflow-cpu           (299 MB smaller; no GPU on Cloud Run)
#   opencv-python     -> opencv-python-headless   (no GUI in a server)
#
# Installing deepface normally would drag the originals back in *alongside* the
# variants, making the image ~310 MB larger rather than smaller. --no-deps is
# what keeps the substitution; its real imports are pinned in requirements.txt.
#
# mtcnn and retina-face are alternative face detectors that this app never uses
# (EmotionService detects with an OpenCV Haar cascade and calls DeepFace with
# detector_backend="skip"). They are still listed because deepface imports them
# eagerly at module load, and they are --no-deps for the same reason as above:
# both depend on `tensorflow` and `opencv-python`.
#
# Adding a package here means checking what it imports at load time. The
# Dockerfile runs a real DeepFace.analyze() at build time so that a missing
# transitive import fails the build instead of the first user's session.
deepface>=0.0.79,<0.1.0
mtcnn>=0.1.0,<2.0.0
retina-face>=0.0.14,<0.1.0

# tf-keras is here for exactly the same reason, and it is easy to miss: it
# declares `tensorflow<2.22,>=2.21`, so installing it normally pulls the full
# x86_64 GPU wheel (1873 MB) back in *on top of* tensorflow-cpu (1273 MB) --
# leaving the image larger than it was before any of this. tensorflow-cpu
# satisfies it in every way that matters, and tensorflow is tf-keras's only
# dependency, so --no-deps loses nothing.
#
# This is invisible on aarch64, where plain `tensorflow` is what gets installed
# anyway. It only appears on the architecture Cloud Run actually runs.
tf-keras>=2.16.0,<3.0.0

# mediapipe, for hand and finger tracking. OpenCV has no hand or finger model;
# see app/services/gesture_service.py.
#
# --no-deps for a sharper version of the same trap as above. mediapipe requires
# `opencv-contrib-python` with no upper bound, which resolves to OpenCV 5 and
# installs a second cv2 *over* the pinned opencv-python-headless. The two
# distributions share files, so the result is not a clean fallback: measured
# here, `import cv2` reported 5.0.0 despite the 4.x pin, and uninstalling
# either one left cv2 broken with no `cvtColor` at all. OpenCV 5 is exactly
# what the pin at the top of requirements.txt exists to keep out.
#
# Verified without it: the hand landmarker runs, and CascadeClassifier -- which
# OpenCV 5 was breaking -- still loads.
#
# Pinned below 1.0 as well: mediapipe 1.x aborts the process on macOS with
# "Check failed: service_ Service is unavailable" from inside its Metal helper,
# an abort rather than an exception, so it takes the server down instead of
# degrading.
mediapipe>=0.10.30,<1.0.0
