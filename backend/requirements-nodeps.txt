# Installed with `pip install --no-deps`, see the Dockerfile.
#
# This file used to hold deepface, mtcnn, retina-face and tf-keras as well, all
# of them --no-deps to stop pip reinstalling the full `tensorflow` and
# `opencv-python` wheels over the -cpu and -headless variants. Emotion
# classification moved to an ONNX model run through cv2.dnn, so tensorflow and
# the four packages that needed that treatment are gone entirely. Only mediapipe
# still needs it, for a different reason.
#
# Adding a package here means checking what it imports at load time: --no-deps
# means pip is no longer checking for you, and the unit suite cannot cover the
# gap because CI installs none of the ML stack. The Dockerfile exercises the
# real entry point at build time so a missing transitive import fails the build
# instead of the first user's session.

# mediapipe, for hand and finger tracking. OpenCV has no hand or finger model;
# see app/services/gesture_service.py.
#
# --no-deps for a sharper version of the same trap as above. mediapipe requires
# `opencv-contrib-python` with no upper bound, which resolves to OpenCV 5 and
# installs a second cv2 *over* the pinned opencv-python-headless. The two
# distributions share files, so the result is not a clean fallback: measured
# here, `import cv2` reported 5.0.0 despite the 4.x pin, and uninstalling
# either one left cv2 broken with no `cvtColor` at all. OpenCV 5 is exactly
# what the pin at the top of requirements.txt exists to keep out.
#
# Verified without it: the hand landmarker runs, and CascadeClassifier -- which
# OpenCV 5 was breaking -- still loads.
#
# Pinned below 1.0 as well: mediapipe 1.x aborts the process on macOS with
# "Check failed: service_ Service is unavailable" from inside its Metal helper,
# an abort rather than an exception, so it takes the server down instead of
# degrading.
mediapipe>=0.10.30,<1.0.0
