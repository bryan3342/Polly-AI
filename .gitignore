# Python
venv/
backend/venv/
*.pyc
__pycache__/
*.egg-info/

# Environment
.env
.env.local
.env.production

# Database
*.db
*.sqlite3

# Dependencies
node_modules/
frontend/node_modules/

# Build output
frontend/dist/
dist/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Jekyll (not used)
_site/
.jekyll-cache/
Gemfile.lock

# DeepFace model weights, downloaded on first run (run-local.sh caches them here,
# and the Dockerfile bakes them into the image). ~6 MB of binary that the code
# fetches for itself; nothing here should be a source artifact.
backend/.deepface/

# MediaPipe hand landmarker, fetched by scripts/fetch_models.py (~7.5 MB).
backend/.models/
